# Local environment variables (kept out of repo).
# This sanitized .env.local contains placeholder values only — replace locally with real secrets.
# NOTE: Do NOT commit real secrets into the repository. Use Vercel / environment management instead.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE=

# Add any other public environment variable names used by the app below as blank placeholders.
